@model IEnumerable<Gestionale_Pizzeria.Models.Ruoli>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TipoRuoli)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TipoRuoli)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdRuoli }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdRuoli }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdRuoli })
        </td>
    </tr>
}

</table>
